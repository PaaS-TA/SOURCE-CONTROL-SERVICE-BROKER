# Scm Manager Server api Info
# Authorization (super admin)
admin.id = {ADMIN-ID}
admin.pwd = {ADMIN-PASSWORD}

# Api
api.base = {API-BASE}
api.users = {API-USERS}
api.repo = {API-REPOSITORIES}

# CF Test
broker.api.version = {BROKER-API-VERSION}
broker.auth.user = {BROKER-AUTH-USER}
broker.auth.pwd = {BROKER-AUTH-PWD}
